<footer class="footer mt-auto py-4 text-white">
<div class="container">
<div class="row align-items-center text-center text-md-start">
      {# 🏫 Brand / Copyright #}
<div class="col-md-4 mb-3 mb-md-0">
<p class="mb-0 small">
&copy; {{ "now"|date("Y") }} <span class="fw-semibold text-highlight">EduTest</span> — Tous droits réservés.
</p>
</div>
 
      {# 🔗 Quick Links #}
<div class="col-md-4 mb-3 mb-md-0">
<a href="{{ path('home') }}" class="footer-link me-3">Accueil</a>
<a href="{{ path('home') }}#features" class="footer-link me-3">Fonctionnalités</a>
<a href="{{ path('home') }}#contact" class="footer-link">Contact</a>
</div>
 
      {# 🌐 Social Media #}
<div class="col-md-4">
<div class="social-icons d-flex justify-content-center justify-content-md-end gap-3">
<a href="#" class="footer-social"><i class="bi bi-facebook"></i></a>
<a href="#" class="footer-social"><i class="bi bi-twitter"></i></a>
<a href="#" class="footer-social"><i class="bi bi-linkedin"></i></a>
</div>
</div>
</div>
</div>
</footer>
 
<style>
.footer {
  background: rgba(10, 15, 25, 0.6);
  backdrop-filter: blur(10px);
  border-top: 1px solid var(--border-color);
  box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.5);
  font-size: 0.95rem;
  color: #dbe4f4;
}
.footer-link {
  color: #bfc8db;
  text-decoration: none;
  transition: color 0.3s ease;
}
.footer-link:hover {
  color: var(--primary-light);
}
.footer-social {
  font-size: 1.25rem;
  color: #bfc8db;
  transition: color 0.3s ease, transform 0.3s ease;
}
.footer-social:hover {
  color: var(--primary-light);
  transform: translateY(-3px);
}
.text-highlight {
  color: var(--primary-light);
}
</style>