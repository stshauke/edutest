{% extends 'base.html.twig' %}
{% block title %}Mes résultats | EduTest{% endblock %}

{% block body %}
<div class="container py-5">
    <h1 class="mb-4 text-center">📈 Mes résultats</h1>

    {% if assignments|length == 0 %}
        <div class="alert alert-info text-center">Aucun examen soumis pour le moment.</div>
    {% else %}
        <table class="table table-striped table-hover shadow-sm">
            <thead class="table-dark">
                <tr>
                    <th>Examen</th>
                    <th>Soumis le</th>
                    <th>Note /20</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for a in assignments %}
                    <tr>
                        <td>{{ a.exam.title }}</td>
                        <td>{{ a.submittedAt ? a.submittedAt|date('d/m/Y H:i') : '-' }}</td>
                        <td>{{ a.finalGrade ?? '-' }}</td>
                        <td>
                            <a href="{{ path('student_result_detail', {id: a.id}) }}" class="btn btn-sm btn-outline-info">
                                📄 Détail
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="pagination d-flex justify-content-center">
            {{ knp_pagination_render(assignments) }}
        </div>
    {% endif %}
</div>
{% endblock %}
