{# templates/_partials/footer.html.twig #}

<footer class="footer border-top bg-white mt-auto">
  <div class="container py-3 d-flex flex-column flex-md-row align-items-center justify-content-between gap-2">

    <div class="d-flex align-items-center gap-2 text-muted small">
      <i class="bi bi-mortarboard-fill text-primary"></i>
      <span>
        © {{ "now"|date("Y") }} EduTest —
        Plateforme d’examens en ligne (projet pédagogique)
      </span>
    </div>

    <div class="d-flex align-items-center gap-3 small">
      <span class="text-muted">Réalisé avec Symfony &amp; MySQL</span>
    </div>

  </div>
</footer>
