<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}EduTest{% endblock %}</title>

  {# 🧱 Bootstrap + Icônes #}
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  {% block stylesheets %}
  <style>
    html, body {
      height: 100%;
      display: flex;
      flex-direction: column;
      background-color: #f8f9fa;
      scroll-behavior: smooth;
    }

    main {
      flex: 1 0 auto;
      padding-top: 5rem;
    }

    .navbar a.nav-link:hover {
      text-decoration: underline;
    }

    .footer {
      background-color: #0d6efd;
      color: white;
      padding: 1.5rem 0;
      text-align: center;
    }
  </style>
  {% endblock %}
</head>

<body class="d-flex flex-column min-vh-100">

  {# ========================== #
     🌐 NAVBAR GLOBALE (Publique)
     -> N’est pas incluse dans l’espace admin
  # ========================== #}
  {% if not app.request.get('_route') starts with 'admin_' %}
    {% include '_partials/navbar.html.twig' %}
  {% endif %}

  {# ========================== #
     🌟 CONTENU PRINCIPAL
  # ========================== #}
  <main class="flex-grow-1 container-fluid px-4">
    {% block body %}{% endblock %}
  </main>

  {# ========================== #
     🌈 FOOTER GLOBAL (toujours affiché)
  # ========================== #}
  {% include '_partials/footer.html.twig' %}

  {# ========================== #
     ⚙️ SCRIPTS JS
  # ========================== #}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  {% block javascripts %}{% endblock %}
</body>
</html>
